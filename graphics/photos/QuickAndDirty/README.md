Naming Exception
======================
`QuickAndDirty Previews.lrdata` is a Lightroom preview cache, generated automatically.
Its name is fixed by Lightroom, so the repository naming rule (lowercase, no spaces) does not apply here.